This directory contains helpful Bash scripts designed to automate tasks, improve productivity, and simplify workflows.

## Disk Burn-In and Testing

[Shell script for burn-in and testing of drives](https://github.com/Spearfoot/disk-burnin-and-testing)

## Find and Find & Replace

The `blank2underscore` script replaces all the blanks in file and directory names with an underscore line `_`. The `underscore2blank` script does the opposite.

The `find_string` searches for a string of characters in file and directory names.

